use crate::config::table_context::TableContext;
use ::config::Config;
use ::config::File;

mod config;
mod extract;
mod load;
mod pipeline;
mod transform;
fn main() {}

#[test]
fn test_hallo_world() {
    main()
}
